name: Kalshi High-Probability Trading Bot

on:
  schedule:
    # Runs every 1 hour
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Execution Mode'
        required: true
        default: 'live'
        type: choice
        options:
          - live
          - paper
      environment:
        description: 'Kalshi API Environment'
        required: true
        default: 'prod'
        type: choice
        options:
          - prod
          - demo

jobs:
  trade-cycle:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Tests
        run: |
          python -m pytest

      - name: Execute Trading Cycle
        env:
          KALSHI_API_KEY_ID: ${{ secrets.KALSHI_API_KEY_ID || secrets.KALSHI_API_KEY || secrets.KALSHI_KEY_ID || secrets.KALSHI_API }}
          KALSHI_PRIVATE_KEY: ${{ secrets.KALSHI_PRIVATE_KEY || secrets.KALSHI_RSA_PRIVATE_KEY || secrets.KALSHI_SECRET_KEY }}
          KALSHI_ENV: ${{ github.event.inputs.environment || 'prod' }}
        run: |
          MODE="${{ github.event.inputs.mode || 'live' }}"
          ENV_NAME="${{ github.event.inputs.environment || 'prod' }}"
          echo "Executing Kalshi Trading Bot in $MODE mode on $ENV_NAME..."
          python main.py bot --mode "$MODE" --env "$ENV_NAME"

      - name: Upload Trading State Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trading-state
          path: "*_trading_state.json"
          if-no-files-found: ignore
